#include <iostream>
using namespace std;
int sum(int x, int y, int z = 0, int w = 0) {
    return x + y + z + w;
}
int main() {
    cout << "Enter the values: ";
    int x, y, z, w;
    cin >> x >> y >> z >> w;
    cout << "Sum: " << sum(x, y, z, w) << endl;
    return 0;
}
