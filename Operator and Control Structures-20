#include <iostream>
using namespace std;
int factorial(int n) {
    int fact = 1;
    for (int i = 1; i <= n; i++) {
        fact *= i;
    }
    return fact;
}
int main() {
    int number, originalNum, digit, sum = 0;
    cout << "Enter a number: ";
    cin >> number;
    originalNum = number;
    while (number > 0) {
        digit = number % 10; 
        sum += factorial(digit);
        number /= 10;  
    }
    if (sum == originalNum) {
        cout << originalNum << " is a strong number." << endl;
    } else {
        cout << originalNum << " is not a strong number." << endl;
    }
    return 0;
}
